{
  "name": "eviomri-arcade",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && npm run build:manifest && vite build && node tools/prune-dist.mjs",
    "build:assets": "node tools/build-assets.mjs && npm run build:manifest",
    "test": "vitest run",
    "test:assets": "node tools/check-assets.mjs",
    "lan": "node server/index.mjs",
    "verify": "npm run build:assets && npm run test:assets && npm test && npm run build",
    "verify:character": "node tools/verify-character.mjs",
    "intake:character": "node tools/intake-character.mjs",
    "readiness": "node tools/readiness.mjs",
    "art:pipeline": "node tools/art-pipeline.mjs",
    "art:queue": "node tools/art-pipeline.mjs --queue",
    "build:manifest": "node tools/build-content-manifest.mjs",
    "android:sync": "npm run build && npx cap sync android",
    "android:apk": "npm run android:sync && cd android && ./gradlew assembleRelease && node ../tools/collect-apk.mjs",
    "android:run": "npm run android:sync && npx cap run android",
    "content:deploy": "npm run build && firebase deploy --only hosting,firestore:rules --account shmulious@gmail.com",
    "analytics:report": "node tools/analytics-report.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/filesystem": "^8.1.3",
    "phaser": "^3.90.0",
    "qrcode": "^1.5.4",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2",
    "@types/node": "^22.10.0",
    "@types/qrcode": "^1.5.6",
    "@types/ws": "^8.18.1",
    "sharp": "^0.35.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.0",
    "vitest": "^4.1.11"
  }
}
