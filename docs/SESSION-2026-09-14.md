# Handoff for the 2026-09-14 session

Prepared 2026-09-13 23:15 on branch `game/core` (HEAD `2f750ea`, not pushed).

## 1. Built and deployed tonight

| what | where | state |
|---|---|---|
| asset pack | `public/game/` (143 files, 38.4 MB) | rebuilt with Shmuel's 2026-kit set and the WIP Pitz set; gates clean |
| tests | `npm test` | 12 files / 143 tests pass, incl. the new `tests/shippedRoster.test.ts` |
| web build | `dist/` (53 MB) | `npm run build`; `public/assets/backups` is now pruned (it was 350 MB inside the last APK) |
| APK | `build/EviOmri-0.3.0-release.apk` (45 MB, versionCode 3, label "EviOmri") | signed with the Shmulious keystore; the old `build/Nepho-0.2.0-release.apk` is still there for comparison |
| content pack | https://nepho-eviomri.web.app/game/ | version `c898717dd0ca284a` released; installed apps pick it up at next boot |

Verified in the browser: tab title, Boot/Lobby caption and the lobby logo read EVIOMRI; all six heroes are in the lobby.

## 2. Characters that still need work — least to most

`docs/prompts/README.md` is the queue (91 requests): **pitz** (1 file — `run.png`) → **railmaw, crown-runner,
the-null, vault-mother, ultra-signal** (7 each) → **brawler, knight, chainer, kicker, shield** (11 each). Every one has
its prompt file in `docs/prompts/`. Start with `npm run art:queue` or hand the files to Codex per `codex-queue.md`.
Shmuel is delivered (`3dce48c`); his old sheet `docs/refs/shmuel-sheet.png` must not be attached any more — the 2026 one is
`docs/refs/shmuel-2026-kit-sheet.png`.

## 3. Roster — max variety by strength

`public/game/roster.json` (commit `8c0868b`): enemies ordered punk → punk-b → gold-sorceress → rainbow-oracle → bio-brute →
void-demon, each level a different mix that widens and hardens with the level (L1 punks only … L10 all six); bosses by
hp/poise: 1 Ferryman · 2 Monk Zero · 3 Glass Warden · 4 Prism Queen · 5 Abyss Dragon · 6 Ferryman (rematch) · 7 Kilnheart ·
8 Flame Samurai · 9 Market King · 10 Storm Colossus. Legacy-art characters stay out until they pass the gate; when one is
delivered, give it its levels on `/backoffice.html` and run `npm test` — `shippedRoster` plays every level with the bot.

## 4. All heroes enabled

Eviatar, Omri, Shmuel, Savta Orly, Saba Kobi, Noa — all `enabled: true` in the roster and shown in the lobby.

## 5. Rebrand → EviOmri

Commit `ca87d75`: page title, rotate prompt, Boot/Lobby caption, `logo.svg` wordmark, `capacitor.config.ts` appName,
Android `strings.xml`, APK file name, package name `eviomri-arcade` 0.3.0, README, backoffice/showcase titles. The launcher
icon and Android splash are the old paintings with the wordmark repainted by a script — good enough to ship, and
`docs/prompts/brand-eviomri.md` has the two prompts to regenerate them properly (originals under
`public/assets/backups/brand-nepho/`). Deliberately unchanged: the Firebase project id `nepho-eviomri`, the `nepho.*`
localStorage keys (best score / sound / control size would reset otherwise), the `__nepho*` dev hooks, the bundle id.

## 6. Level locations (geometry changed 00:10 — read this)

**Backdrops are now 4:1 band plates** (2800×700, `ART_BAND` 1400×350 world at (180,178)): ≥90% of the art's height is
on screen at any moment and a level can have up to 5 waves (`MAX_WAVES`, `SEGMENT_STEP` 200 — a 3-wave level scrolls
400, boss where the last wave was). Legacy 941×334 plates keep working through the catalog's per-level `art` rect until
replaced. Each plate is two 21:9 generations stitched with `tools/stitch-backdrop.mjs`; `tools/place-backdrop.mjs` now
writes `public/assets/generated/backdrops/level-NN.png` and the build picks it up. `tests/artBand.test.ts` pins the
geometry. The Gemini brief (`docs/prompts/gemini-backdrops.md`) is updated: 20 requests for the ten levels.


`docs/locations/README.md` + ten `level-NN-<id>.md` files: the real place, a Street View link, reference frames/photos in
`docs/refs/locations/`, what the current plate gets wrong, and a paste-ready prompt. Delivery: `node tools/place-backdrop.mjs
<level> <image>` → `npm run build:assets`.

Pinned tonight with real Street View frames (rendered to disk by the new `tools/streetview-ref.mjs` from the pano tiles):
1 **HaSarig 31, Rishon LeZion** (owner's correction), 2 Refael Eitan St (owner confirmed the view), 3 Sagrada Família /
Eixample, 4 Plaça d'Octavià; 5 **Col·legi Hatikva, Valldoreix** (owner's correction) uses the school's own website photos
of the court, yard and building — the ones showing pupils are gitignored (`people-*`, URLs kept). 10 Catalunya keeps the
Commons photos (the bunkers have only user photospheres). Levels 6–9 (capoeira gym, basketball hall, theatre, candy
factory) are **imaginary by your decision** — each file invents the place in detail and the prompt runs as-is. Nothing left
to confirm; all ten can be generated.

## Loose ends

- Not pushed. `git push origin game/core` when you want it on GitHub (personal account, `shmulious-student`).
- Pitz ships with the failing `run.png` (2 of 9 frames bob) — the fix is queue #1.
- `readiness` still says 20 READY / 1 FAILED / 10 LEGACY.
